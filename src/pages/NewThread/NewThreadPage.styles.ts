import { createStyles } from "@mantine/core";

const useNewThreadPageStyles = createStyles((theme) => ({
  title: {
    fontFamily: "proxima-nova",
    fontSize: "36px",
    color: theme.colors["cool-grey"][6],
  },
}));

export default useNewThreadPageStyles;
