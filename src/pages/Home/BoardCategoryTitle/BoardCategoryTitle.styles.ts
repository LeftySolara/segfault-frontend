import { createStyles } from "@mantine/core";

const useCategoryTitleStyles = createStyles((theme) => ({
  "category-title": {
    marginBottom: "24px",
    padding: 0,
    fontFamily: "soleil",
    color: theme.colors.purple[7],
  },
}));

export default useCategoryTitleStyles;
